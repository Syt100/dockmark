{
  "name": "@dockmark/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"No extension tests yet\"",
    "lint": "corepack pnpm run lint:check",
    "lint:check": "echo \"No extension lint yet\"",
    "lint:fix": "echo \"No extension lint fix yet\"",
    "format": "echo \"No extension format yet\""
  },
  "devDependencies": {
    "typescript": "~6.0.0"
  }
}
