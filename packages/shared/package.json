{
  "name": "@dockmark/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "echo \"No shared lint yet\"",
    "format": "echo \"No shared format yet\""
  },
  "devDependencies": {
    "typescript": "~6.0.0",
    "vitest": "^4.1.4"
  }
}
